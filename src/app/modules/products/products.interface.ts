export type TProduct= {
    name: string;
    price: number;
    description?: string; 
    quantity:number;
    category: string; // Category for the product (e.g., clothing, electronics)
    brand: string; // Brand of the product
    // createdAt?: Date; // Timestamp for product creation
    // updatedAt?: Date; // Timestamp for product update
   
  }